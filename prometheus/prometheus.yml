global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "wifi-monitor"
    static_configs:
      - targets: ["localhost:8501"]
        labels:
          service: "wifi-monitor"
